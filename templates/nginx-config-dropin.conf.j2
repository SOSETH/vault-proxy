upstream vaultbackend {
{% for item in vault_server_ips %}
    server {{ item }}:8443 max_conns=50;
{% endfor %}
}

proxy_cache_path  /var/lib/nginx/cache  levels=1:2 keys_zone=STATIC:10m inactive=24h  max_size=1g;